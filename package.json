{
  "name": "checkpoint",
  "version": "0.1.0",
  "private": true,
  "description": "CheckPoint - Social presence platform with contextual matching",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "postinstall": "cd apps/api && npm install",
    "build": "cd apps/api && npm run build",
    "start": "cd apps/api && npm run start:prod"
  }
}
