{
  "name": "@checkpoint/types",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@checkpoint/config": "workspace:*",
    "typescript": "^5.4.0",
    "rimraf": "^5.0.5"
  }
}
